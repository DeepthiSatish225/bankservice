package com.example.demo.UserAccount;
import org.springframework.context.annotation.Bean;
import org.springframework.context.annotation.Configuration;
//import org.springframework.security.config.annotation.web.builders.HttpSecurity;
//import org.springframework.security.web.SecurityFilterChain;

@Configuration
public class SecurityConfig {
//    @Bean
//    public <SecurityFilterChain> SecurityFilterChain securityFilterChain(HttpSecurity http) throws Exception {
//        http
//                .cors().and()
//                .csrf().disable() // Disable CSRF for testing (not recommended in production)
//                .authorizeHttpRequests(auth -> auth
//                        .requestMatchers("/api/v1/useraccount").permitAll() // Allow public access
//                        .anyRequest().authenticated()
//                );
//        return http.build();
//    }
}
 
